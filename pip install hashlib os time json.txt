pip install hashlib os time json



def check_file_integrity(file_path, original_hash):
    new_hash = calculate_file_hash(file_path)
    if not new_hash:
        return
    if new_hash == original_hash:
        print(f"No changes detected in '{file_path}'.")
    else:
        print(f"Changes detected in '{file_path}'!")            

#if__name__ == "__main__":
    file_path = input("enter the file path to check integrity: ")
    initial_hash = calculate_file_hash(file)

    if initial_hash:
        print(f"Initial file hash: {initial_hash}")
        print("Monitoring changes. Type 'exit' to stop.")

        while True:
            user_input = input("press Enter to check file integrity or type 'exit': ")
            if user_input.lower() == "exit":
                break
            check_file_integrity(file_path, initial_hash)